from tse.epson import TSE


def enable_tse(app, state):
    ...
    # if app.config.getboolean('tse', 'enabled'):
    #     app.tse = TSE(
    #         app.config.get('tse', 'id'),
    #         app.config.get('tse', 'host')
    #     )
